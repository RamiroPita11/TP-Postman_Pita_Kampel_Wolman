const PI = 3.14;

function sumar(x, y) {
    return x + y;
}

function restar(x, y) {
    return x - y;
}

function multiplicar (a, b){
    return a * b;
} 
function dividir (a, b){
    return a / b;
} 

export {PI, sumar, restar, multiplicar, dividir};